{% extends 'base.html.twig' %}

{% block body %}
    <form method="post" action="{{ url('round_delete', { id: round.id }) }}">
        Are you sure you want to delete {{ round.name }}?
        <input type="submit" value="Yes"> | <a href="{{ url('round_detail', { id: round.id }) }}">No</a>
    </form>
{% endblock %}