{% extends '::base.html.twig' %}

{% block title %}
    Change Password
{% endblock %}

{% block body %}
    <h1>Change Password</h1>
    <h2>{{ person.name }}</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <input type="submit" value="Save"/>
    {{ form_end(form) }}
{% endblock %}
