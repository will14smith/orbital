{% extends '::base.html.twig' %}

{% block title %}
    Create Score
{% endblock %}

{% block body %}
    {{ form_start(form) }}

    {{ form_row(form.date_shot) }}

    {{ form_row(form.round) }}
    {{ form_row(form.score) }}

    {# WHAT was shot #}
    {# WHO shot it #}


    {{ form_widget(form) }}

    <input type="submit" value="Create"/>
    {{ form_end(form) }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script>
        'use strict';

        (function ($) {
            $('#score_proof_proof_images').collection('Image');
            $('#score_proof_proof_people').collection('Person');
        })(jQuery);
    </script>

{% endblock %}
