{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ club.name }}</h1>

    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ url('club_edit', { 'id': club.id }) }}">Edit</a>
        | <a href="{{ url('club_delete', { 'id': club.id }) }}">Delete</a>
    {% endif %}

    <p>
        TODO add the fields!
    </p>
{% endblock %}